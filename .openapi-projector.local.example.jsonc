{
  // 이 파일은 실행한 프론트엔드 프로젝트 루트 기준 로컬 설정입니다.
  // 보통 projectRoot 는 현재 디렉터리를 뜻하는 "." 그대로 둡니다.
  "projectRoot": ".",
  "initDefaults": {
    // init 시 openapi/config/project.jsonc 의 기본값으로 채웁니다.
    // Swagger UI 주소가 아니라 OpenAPI JSON 요청 URL(예: /v3/api-docs)을 적습니다.
    "sourceUrl": ""
  }
}
